# OPTIONAL — only for the advanced agent.py automation. The course does NOT need this.
# Your Claude.ai subscription (Cowork) needs no API key. This key is separate from your
# subscription and is billed separately. Only fill it in if you run the Python version.
ANTHROPIC_API_KEY=your_api_key_here
